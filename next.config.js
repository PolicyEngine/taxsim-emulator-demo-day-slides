/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/taxsim-emulator-demo-day-slides',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
